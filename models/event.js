class Event {
  constructor(id, imageUri, title, date, description) {
    this.id = id;
    this.imageUri = imageUri;
    this.title = title;
    this.date = date;
    this.description = description;
  }
}

export default Event;
